import type { ExtensionAPI } from '@earendil-works/pi-coding-agent';

/**
 * Pi extension entry point. Implement registration exactly as specified in
 * docs/09-implementation-blueprint.md after copying this scaffold.
 */
export default function signalBoardExtension(_pi: ExtensionAPI): void {
  // The build agent replaces this no-op with the M0 registration shell before release.
}
